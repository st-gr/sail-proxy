meta {
  name: Delete AWS Credentials
  type: http
  seq: 8
}

post {
  url: {{base_url}}/deleteAwsCredentials
  body: json
  auth: basic
}

auth:basic {
  username: admin@test.com
  password: admin
}

body:json {
  {
    "accessKeyId": "{{created_aws_access_key_id}}"
  }
}

assert {
  res.status: eq 200
}

script:post-response {
  console.log('✓ AWS credentials deleted successfully');
  console.log('Success:', res.body.success);
  console.log('Message:', res.body.message);
}