# SAP LLM Gateway OAuth2 Demo - Okta SAML Integration Environment
# =================================================================
# Copy this to .env and configure for your Okta environment

# =================================================================
# NGINX CONFIGURATION
# =================================================================
# Nginx HTTP port (configurable to avoid conflicts with Traefik)
NGINX_HTTP_PORT=8080
# Domain name for nginx (localhost for dev, custom domain for prod)
NGINX_DOMAIN=localhost

# =================================================================
# DATABASE CONFIGURATION (Shared PostgreSQL)
# =================================================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=sap_llm_gateway
POSTGRES_USER=admin_user
POSTGRES_PASSWORD=admin_password

# =================================================================
# IDENTITY PROVIDER CONFIGURATION
# =================================================================
# Identity provider selection: okta (for this setup)
IDENTITY_PROVIDER=okta

# =================================================================
# OKTA SAML CONFIGURATION
# =================================================================
# Okta SAML application settings
OKTA_METADATA_URL=OKTA_METADATA_URL
OKTA_SSO_URL=OKTA_SSO_URL
OKTA_REDIRECT_URI=http://localhost:8080/dex/callback
OKTA_ENTITY_ISSUER=http://localhost:8080/dex

# SAML certificate (base64 encoded or file path)
OKTA_SAML_CA_DATA="OKTA_SAML_CA_DATA"
OKTA_SAML_CA_FILE=/etc/dex/saml-ca.pem

# Group and user attribute mappings
OKTA_USERNAME_ATTR=name
OKTA_EMAIL_ATTR=email
OKTA_GROUPS_ATTR=groups
OKTA_NAME_ATTR=name

# =================================================================
# GROUP MAPPING CONFIGURATION
# =================================================================
# Map external groups/users to internal application roles
# Format: JSON object with "external_group": "internal_role" mappings

# Local Users (Development Only - not used in Okta mode)
LOCAL_USER_MAPPING={}

# GitHub Groups (not used in Okta mode)
GITHUB_GROUP_MAPPING={}

# LDAP Groups (not used in Okta mode)
LDAP_GROUP_MAPPING={}

# Okta Groups (Production - Maps Okta group to role)
OKTA_GROUP_MAPPING={"sap-llm-gateway-admin":"admin","sap-llm-gateway-user":"user","sap-llm-gateway-readonly":"readonly","Everyone":"user"}

# =================================================================
# OAUTH2-PROXY CONFIGURATION
# =================================================================
OAUTH2_PROXY_PROVIDER=oidc
OAUTH2_PROXY_CLIENT_ID=oauth2-proxy
OAUTH2_PROXY_CLIENT_SECRET=oauth2-proxy-secret
OAUTH2_PROXY_COOKIE_SECRET=WVpQd2VHbHpRV0Z6UkZOSlJFRnRVSE5ZVWxWS1NuTT0=

# Basic settings
OAUTH2_PROXY_HTTP_ADDRESS=0.0.0.0:4180
OAUTH2_PROXY_EMAIL_DOMAINS=yourcompany.com
OAUTH2_PROXY_SET_XAUTHREQUEST=true
OAUTH2_PROXY_PASS_ACCESS_TOKEN=true
OAUTH2_PROXY_COOKIE_SECURE=false

# Redirect URL (nginx routing)
OAUTH2_PROXY_REDIRECT_URL=http://localhost:8080/auth/callback

# OIDC configuration (Dex integration)
OAUTH2_PROXY_OIDC_ISSUER_URL=http://localhost:8080/dex
OAUTH2_PROXY_OIDC_GROUPS_CLAIM=groups
OAUTH2_PROXY_OIDC_EMAIL_CLAIM=email
OAUTH2_PROXY_SCOPE=openid email profile groups

# Debugging (set to info/warn for production)
OAUTH2_PROXY_LOGGING_LEVEL=debug
OAUTH2_PROXY_REQUEST_LOGGING=true

# Advanced OIDC settings - Required for proper Dex integration
OAUTH2_PROXY_SKIP_OIDC_DISCOVERY=true
OAUTH2_PROXY_LOGIN_URL=http://localhost:8080/dex/auth
OAUTH2_PROXY_REDEEM_URL=http://nginx:8080/dex/token
OAUTH2_PROXY_OIDC_JWKS_URL=http://nginx:8080/dex/keys
OAUTH2_PROXY_PROFILE_URL=http://nginx:8080/dex/userinfo
OAUTH2_PROXY_SET_AUTHORIZATION_HEADER=true
OAUTH2_PROXY_PASS_USER_HEADERS=true

# Additional oauth2-proxy settings
OAUTH2_PROXY_INSECURE_OIDC_SKIP_ISSUER_VERIFICATION=false
OAUTH2_PROXY_OIDC_EXTRA_AUDIENCES=oauth2-proxy

# =================================================================
# APPLICATION CONFIGURATION
# =================================================================
# Node.js environment
NODE_ENV=production
PORT=4004