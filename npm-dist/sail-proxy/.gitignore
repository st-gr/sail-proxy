# Dependencies
node_modules/

# Build outputs
dist/
bundled/
# The packed tarball CI's Phase 6.5 produces (pack + install + run +
# inference + stop against the real CLI). Regenerated every run, ~31 MB.
*.tgz

# Logs
*.log
logs/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp

# Auth (never commit publish tokens)
.npmrc